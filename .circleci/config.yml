version: 2.1

jobs:
  test_git:
    docker:
      - image: cimg/base:2023.06
    steps:
      - checkout
      - run:
          name: we are just testing it 
          command: |
            git clone https://github.com/AnkitJodhani/temp2.git
            cd temp2
            sed -i "s/TAG/build-$CIRCLE_BUILD_NUM/g" deployment.yaml
            git add .
            git commit -m "new build with imgTAG build-$CIRCLE_BUILD_NUM"
            git config credential.helper 'cache --timeout=120'
            git config user.email "ankitjodhani1903@gmail.com"
            git config user.name "AnkitJodhani"
            git push -q https://$GITHUB_PERSONAL_TOKEN@git@github.com:AnkitJodhani/temp2.git main 
workflows:
  testing:
    jobs:
      - test_git

  